import 'package:flutter/material.dart';

class CleanerManagementScreen extends StatelessWidget {
  const CleanerManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Cleaner Management"),
    );
  }
}
